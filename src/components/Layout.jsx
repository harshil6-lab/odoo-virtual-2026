import {Outlet,NavLink,Link} from 'react-router-dom';import {CalendarDays,Compass,Luggage,Menu,X,Users} from 'lucide-react';import {useState} from 'react';import {useAuth} from '../lib/auth';import {getInitials} from '../lib/utils';import {GlobeTrotterLogo,Footer} from './UI';
const links=[[Luggage,'Journeys','/app/trips'],[Compass,'Explore','/app/explore'],[CalendarDays,'Calendar','/app/dashboard'],[Users,'Community','/app/community']];
export default function Layout(){const [open,setOpen]=useState(false);const {user}=useAuth();return <div className="min-h-screen flex flex-col"><header className="sticky top-0 z-40 h-16 bg-canvas/90 backdrop-blur border-b border-border"><div className="h-full max-w-[1500px] mx-auto px-4 lg:px-8 flex items-center justify-between"><Link to="/app/dashboard"><GlobeTrotterLogo/></Link><nav className="hidden md:flex items-center gap-1">{links.map(([I,l,to])=><NavLink key={l} to={to} className={({isActive})=>`flex items-center gap-2 px-4 py-2 rounded-lg text-sm font-semibold ${isActive?'bg-white shadow-sm':'text-muted hover:text-ink'}`}><I size={17}/>{l}</NavLink>)}</nav><div className="flex items-center gap-2"><Link to="/app/trips/new" className="btn-primary hidden sm:inline-flex h-10">+ Plan</Link><Link to="/app/profile" className="size-10 rounded-full bg-ink text-white grid place-items-center font-bold text-sm">{getInitials(user?.user_metadata?.name||user?.name||user?.email)}</Link><button className="icon-btn md:hidden" onClick={()=>setOpen(!open)} aria-label="Menu">{open?<X/>:<Menu/>}</button></div></div>{open&&<nav className="md:hidden bg-white border-b p-3 shadow-soft">{links.map(([I,l,to])=><NavLink onClick={()=>setOpen(false)} key={l} to={to} className="flex items-center gap-3 p-3 rounded-lg font-semibold"><I size={18}/>{l}</NavLink>)}</nav>}</header><main className="flex-1"><Outlet/></main><Footer/></div>}
