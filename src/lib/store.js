import {CITY_SEED,ACTIVITY_SEED} from './constants';import {uid} from './utils';import {supabase,isSupabaseConfigured} from './supabase'
const key='gt_trips'; const read=()=>JSON.parse(localStorage.getItem(key)||'[]'); const write=x=>localStorage.setItem(key,JSON.stringify(x));
const select='*,trip_stops(*,city:cities(*),trip_activities(*,activity:activities(*))),trip_expenses(*)';
export async function listTrips(userId){if(isSupabaseConfigured){const {data,error}=await supabase.from('trips').select(select).eq('user_id',userId).order('start_date');if(error)throw error;return data||[]}return read()}
export async function getTrip(id){if(isSupabaseConfigured){const {data,error}=await supabase.from('trips').select(select).eq('id',id).single();if(error)throw error;return data}return read().find(t=>t.id===id)}
export async function saveTrip(trip,userId){const now=new Date().toISOString(),id=trip.id||uid();const payload={id,user_id:userId||'demo-user',name:trip.name,description:trip.description||null,start_date:trip.start_date,end_date:trip.end_date,cover_image:trip.cover_image||null,budget:Number(trip.budget)||0,is_public:Boolean(trip.is_public),updated_at:now};if(isSupabaseConfigured){const {data,error}=await supabase.from('trips').upsert(payload).select().single();if(error)throw error;const stops=trip.trip_stops||[];if(stops.length){const stopRows=stops.map((s,i)=>({id:s.id,trip_id:id,city_id:s.city_id,start_date:s.start_date||null,end_date:s.end_date||null,position:i}));const {error:e}=await supabase.from('trip_stops').upsert(stopRows);if(e)throw e}const keepStops=stops.map(s=>s.id);let stale=supabase.from('trip_stops').delete().eq('trip_id',id);if(keepStops.length)stale=stale.not('id','in',`(${keepStops.join(',')})`);await stale;for(const s of stops){const acts=s.trip_activities||[];if(acts.length){const rows=acts.map(a=>({id:a.id,trip_stop_id:s.id,activity_id:a.activity_id,scheduled_date:a.scheduled_date||null,scheduled_time:a.scheduled_time||null,custom_cost:Number(a.custom_cost??a.activity?.cost??0),notes:a.notes||null}));const {error:e}=await supabase.from('trip_activities').upsert(rows);if(e)throw e}const ids=acts.map(a=>a.id);let del=supabase.from('trip_activities').delete().eq('trip_stop_id',s.id);if(ids.length)del=del.not('id','in',`(${ids.join(',')})`);await del}const expenses=trip.trip_expenses||[];if(expenses.length){const rows=expenses.map(e=>({id:e.id,trip_id:id,category:e.category,description:e.description||'',amount:Number(e.amount)||0,date:e.date||null}));const {error:e}=await supabase.from('trip_expenses').upsert(rows);if(e)throw e}const expIds=expenses.map(e=>e.id);let expDel=supabase.from('trip_expenses').delete().eq('trip_id',id);if(expIds.length)expDel=expDel.not('id','in',`(${expIds.join(',')})`);await expDel;return {...data,trip_stops:stops,trip_expenses:expenses}}const full={...trip,...payload,created_at:trip.created_at||now};const all=read();const i=all.findIndex(x=>x.id===id);if(i>=0)all[i]=full;else all.unshift(full);write(all);return full}
export async function deleteTrip(id){if(isSupabaseConfigured){const {error}=await supabase.from('trips').delete().eq('id',id);if(error)throw error;return}write(read().filter(t=>t.id!==id))}
export const getCities=async()=>CITY_SEED;export const getActivities=async()=>ACTIVITY_SEED;
