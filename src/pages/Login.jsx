import {useState} from 'react';import {Link,useNavigate} from 'react-router-dom';import {ArrowRight,Eye,EyeOff} from 'lucide-react';import {useAuth} from '../lib/auth';import {supabase,isSupabaseConfigured} from '../lib/supabase';import {toast} from 'sonner';import {ImageWithFallback,GlobeTrotterLogo} from '../components/UI';
export default function Login({mode}){const signup=mode==='signup',reset=mode==='reset',nav=useNavigate(),{login,signup:sign}=useAuth();const [form,setForm]=useState({first:'',last:'',email:'',password:'',confirm:''}),[show,setShow]=useState(false),[busy,setBusy]=useState(false),[oauthBusy,setOauthBusy]=useState(false);const update=e=>setForm({...form,[e.target.name]:e.target.value});const submit=async e=>{e.preventDefault();if(reset){setBusy(true);try{if(!isSupabaseConfigured)throw new Error('Password reset requires Supabase configuration.');const {error}=await supabase.auth.resetPasswordForEmail(form.email,{redirectTo:`${location.origin}/login`});if(error)throw error;toast.success('Reset link sent. Check your email.');nav('/login')}catch(err){toast.error('Unable to send reset link. Check the email and try again.')}finally{setBusy(false)}return}if(form.password.length<6)return toast.error('Password must be at least 6 characters');if(signup&&form.password!==form.confirm)return toast.error('Passwords do not match');setBusy(true);try{if(signup){await sign(form.email,form.password,`${form.first} ${form.last}`.trim())}else await login(form.email,form.password);toast.success(signup?'Welcome to GlobeTrotter':'Welcome back');nav('/app/dashboard')}catch(err){toast.error(signup?'Unable to create account. Please check your details.':'Invalid email or password.')}finally{setBusy(false)}};const title=reset?'Forgot your password?':signup?'Create your account':'Welcome back';return <div className="min-h-screen grid lg:grid-cols-2"><div className="hidden lg:block relative"><ImageWithFallback src="https://images.unsplash.com/photo-1500534623283-312aade485b7?auto=format&fit=crop&w=1600&q=85" alt="Travelers exploring a mountain destination" className="absolute inset-0 w-full h-full object-cover"/><div className="absolute inset-0 bg-black/30"/><div className="absolute left-12 bottom-12 text-white max-w-md"><p className="text-3xl font-extrabold">The world is wide.<br/>Your plan should be too.</p><p className="mt-3 text-white/80">Make every trip count with a beautifully organized itinerary.</p></div></div><div className="grid place-items-center p-6 py-10"><div className="w-full max-w-xl"><Link to="/" className="inline-flex mb-10"><GlobeTrotterLogo/></Link><h1 className="text-3xl font-extrabold">{title}</h1><p className="text-black/50 mt-2">{reset?"Enter your email and we'll send you a link to reset your password.":signup?'Start planning journeys worth remembering.':'Your next adventure is waiting.'}</p><button type="button" disabled={oauthBusy||busy} onClick={async()=>{if(!isSupabaseConfigured)return toast.error("Google sign-in requires Supabase configuration.");setOauthBusy(true);try{const {error}=await supabase.auth.signInWithOAuth({provider:"google",options:{redirectTo:`${location.origin}/app/dashboard`}});if(error)throw error}catch{toast.error("Google sign-in couldn't be completed. Please try again.");setOauthBusy(false)}}} className="btn-secondary w-full mt-8 justify-center">{oauthBusy?"Signing in...":"Continue with Google"}</button><div className="flex items-center gap-3 my-5 text-xs text-black/40"><span className="h-px bg-black/10 flex-1"/>or continue with<span className="h-px bg-black/10 flex-1"/></div><form onSubmit={submit} className="mt-8 grid sm:grid-cols-2 gap-4">{reset?<div className="sm:col-span-2"><label className="label">Email address</label><input required type="email" name="email" className="field" value={form.email} onChange={update} placeholder="you@example.com"/></div>:<>{signup&&<><div><label className="label">First name</label><input required name="first" className="field" value={form.first} onChange={update} placeholder="Alex"/></div><div><label className="label">Last name</label><input required name="last" className="field" value={form.last} onChange={update} placeholder="Morgan"/></div></>}<div className="sm:col-span-2"><label className="label">Email address</label><input required type="email" name="email" className="field" value={form.email} onChange={update} placeholder="you@example.com"/></div><div className="sm:col-span-2"><label className="label">Password</label><div className="relative"><input required type={show?'text':'password'} name="password" className="field pr-12" value={form.password} onChange={update} placeholder="At least 6 characters"/><button type="button" onClick={()=>setShow(!show)} className="absolute right-3 top-3 text-black/40">{show?<EyeOff size={18}/>:<Eye size={18}/>}</button></div></div>{signup&&<><div className="sm:col-span-2"><label className="label">Confirm password</label><input required type="password" name="confirm" className="field" value={form.confirm} onChange={update}/></div></>}</>}</form><button disabled={busy} onClick={submit} className="btn-primary w-full mt-5">{busy?'Workingâ€¦':reset?'Send reset link':signup?'Create account':'Log in'} <ArrowRight size={17}/></button>{!reset&&!signup&&<Link to="/reset" className="block text-sm text-accent font-bold mt-4">Forgot your password?</Link>}<p className="text-sm text-black/50 mt-6 text-center">{reset?<Link className="font-bold text-accent" to="/login">â† Back to login</Link>:signup?'Already have an account? ':'New to GlobeTrotter? '} {!reset&&<Link className="font-bold text-accent" to={signup?'/login':'/signup'}>{signup?'Log in':'Create an account'}</Link>}</p></div></div></div>}




