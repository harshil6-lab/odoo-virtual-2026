import {useState} from 'react';import {Link,useNavigate} from 'react-router-dom';import {Compass,Eye,EyeOff,ArrowRight} from 'lucide-react';import {useAuth} from '../lib/auth';import {toast} from 'sonner';import {SmartImage} from '../components/UI';
export default function Login({mode}){const signup=mode==='signup',nav=useNavigate(),{login,signup:sign}=useAuth();const [form,setForm]=useState({name:'',email:'',password:''}),[show,setShow]=useState(false),[busy,setBusy]=useState(false);const submit=async e=>{e.preventDefault();if(form.password.length<6)return toast.error('Password must be at least 6 characters');setBusy(true);try{signup?await sign(form.email,form.password,form.name):await login(form.email,form.password);toast.success(signup?'Welcome to GlobeTrotter':'Welcome back');nav('/app/dashboard')}catch(err){toast.error(err.message||'Unable to continue')}finally{setBusy(false)}};return <div className="min-h-screen grid lg:grid-cols-2"><div className="hidden lg:block relative"><SmartImage src="https://images.unsplash.com/photo-1500534623283-312aade485b7?auto=format&fit=crop&w=1600&q=85" className="absolute inset-0 w-full h-full object-cover"/><div className="absolute inset-0 bg-black/25"/><div className="absolute left-12 bottom-12 text-white max-w-md"><p className="text-3xl font-extrabold">The world is wide.<br/>Your plan should be too.</p><p className="mt-3 text-white/80">Make every trip count with a beautifully organized itinerary.</p></div></div><div className="grid place-items-center p-6"><div className="w-full max-w-md"><Link to="/" className="flex items-center gap-2 font-extrabold text-xl mb-12"><span className="size-9 rounded-lg bg-accent text-white grid place-items-center"><Compass size={20}/></span>GlobeTrotter</Link><h1 className="text-3xl font-extrabold">{signup?'Create your account':'Welcome back'}</h1><p className="text-black/50 mt-2">{signup?'Start planning your next great escape.':'Your next adventure is waiting.'}</p><form onSubmit={submit} className="mt-8 space-y-4">{signup&&<div><label className="label">Your name</label><input required className="field" value={form.name} onChange={e=>setForm({...form,name:e.target.value})} placeholder="Alex Morgan"/></div>}<div><label className="label">Email</label><input required type="email" className="field" value={form.email} onChange={e=>setForm({...form,email:e.target.value})} placeholder="you@example.com"/></div><div><label className="label">Password</label><div className="relative"><input required type={show?'text':'password'} className="field pr-12" value={form.password} onChange={e=>setForm({...form,password:e.target.value})} placeholder="••••••••"/><button type="button" onClick={()=>setShow(!show)} className="absolute right-3 top-3 text-black/40">{show?<EyeOff size={18}/>:<Eye size={18}/>}</button></div></div><button disabled={busy} className="btn-primary w-full">{busy?'Working…':signup?'Create account':'Log in'} <ArrowRight size={17}/></button></form><p className="text-sm text-black/50 mt-6 text-center">{signup?'Already have an account? ':'New to GlobeTrotter? '}<Link className="font-bold text-accent" to={signup?'/login':'/signup'}>{signup?'Log in':'Sign up'}</Link></p></div></div></div>}
