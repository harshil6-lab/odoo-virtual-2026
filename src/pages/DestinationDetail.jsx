import {useEffect,useState} from 'react';import {Link,useParams} from 'react-router-dom';import {ArrowLeft,Plus,Star} from 'lucide-react';import {getCity,getActivities} from '../lib/store';import {enrichCity,enrichActivity} from '../lib/exploreContent';import {PageHeader,SmartImage,Loading} from '../components/UI';import {money} from '../lib/utils';
export default function DestinationDetail(){const {cityId}=useParams();const [city,setCity]=useState(null),[acts,setActs]=useState([]);useEffect(()=>{Promise.all([getCity(cityId),getActivities()]).then(([c,a])=>{setCity(c);setActs(a.filter(x=>x.city_id===cityId))})},[cityId]);if(!city)return <Loading/>;const x=enrichCity(city);return <div className="page"><Link to="/app/explore" className="text-sm text-black/50 inline-flex gap-2"><ArrowLeft size={15}/>Explore</Link><p className="text-accent text-sm font-bold uppercase tracking-widest mt-8">{city.country}</p><h1 className="text-5xl lg:text-7xl font-extrabold mt-2">{city.name}</h1><p className="text-lg max-w-2xl text-black/55 mt-4">{x.description}</p><button className="btn-primary mt-6"><Plus size={18}/>Add {city.name} to trip</button><div className="grid grid-cols-2 lg:grid-cols-4 gap-3 mt-10">{x.gallery_images.map((img,i)=><SmartImage key={img} src={img} alt={`${city.name} view ${i+1}`} className={`${i===0?'col-span-2 row-span-2':''} w-full aspect-[4/3] object-cover rounded-sm`}/>)}</div><section className="max-w-3xl py-14"><h2 className="text-3xl font-bold">About {city.name}</h2><p className="text-lg leading-8 text-black/65 mt-4">{x.description}</p><div className="grid grid-cols-2 sm:grid-cols-4 gap-5 border-y border-black/10 py-6 mt-8 text-sm"><span><b>Country</b><br/>{city.country}</span><span><b>Cost index</b><br/>{city.cost_index}/5</span><span><b>Popularity</b><br/><Star size={14} className="inline"/> {city.popularity}</span><span><b>Experiences</b><br/>{acts.length}</span></div></section><PageHeader title={`Experiences in ${city.name}`} description="Ideas worth making time for."/><div className="grid md:grid-cols-3 gap-6">{acts.map(a=>{const y=enrichActivity(a,city);return <Link to={`/explore/activities/${a.id}`} key={a.id}><SmartImage src={y.hero_image} alt={a.name} className="w-full aspect-[4/3] object-cover rounded-sm"/><h3 className="font-bold text-lg mt-3">{a.name}</h3><p className="text-sm text-black/50 capitalize">{a.category} · {a.duration_minutes} min · {money(a.cost)}</p></Link>})}</div></div>}
